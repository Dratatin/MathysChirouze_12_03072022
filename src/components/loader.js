/**
 * Component of Loader displays a circle 
 * that is charging
 *
 * @returns {JSX.Element} A Loader component
 */
const Loader = () => {
    return (
        <div className="loader"></div>
    )
}

export default Loader
